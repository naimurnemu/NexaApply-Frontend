import React from 'react';
import { Outlet } from 'react-router-dom';

const AuthContainer = () => {
  return (
    <div>
      <div>
        
      </div>
      <Outlet />
    </div>
  );
};

export default AuthContainer;